name: Build Desktop Applications (Windows, Linux, macOS)

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # ---------------------------------------------------------
  # 1. WINDOWS BUILD
  # ---------------------------------------------------------
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        shell: pwsh
        run: |
          $ver = "${{ github.ref_name }}" -replace '^v', ''
          if ([string]::IsNullOrWhiteSpace($ver)) { $ver = "0.0.0-dev" }
          Set-Content -Path VERSION -Value $ver -NoNewline -Encoding ascii

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r subtranscribe/requirements.txt
          pip install pyinstaller

      - name: Generate app icon from logo.png
        run: |
          python -c "from PIL import Image; img = Image.open('subtranscribe/assets/logo.png'); img.save('subtranscribe/assets/logo.ico', format='ICO', sizes=[(16,16),(32,32),(48,48),(64,64),(128,128),(256,256)])"

      - name: Build with PyInstaller
        run: >
          pyinstaller
          --name "SubTranscribeStudio"
          --windowed
          --onedir
          --icon "subtranscribe/assets/logo.ico"
          --collect-all PySide6
          --collect-all faster_whisper
          --collect-all ctranslate2
          --collect-all deep_translator
          --collect-all tiktoken
          --collect-all tokenizers
          --collect-all sounddevice
          --hidden-import faster_whisper
          --hidden-import ctranslate2
          --clean
          --noconfirm
          main.py

      - name: Bundle runtime assets
        shell: pwsh
        run: |
          # assets/ lives inside subtranscribe/ in the repo (colocated with the
          # code that uses it) but ships flat as dist/SubTranscribeStudio/assets,
          # matching what installer.iss's {app}\assets\... references expect.
          Copy-Item -Recurse -Force subtranscribe/assets dist/SubTranscribeStudio/assets
          if (Test-Path bin) { Copy-Item -Recurse -Force bin dist/SubTranscribeStudio/bin }
          Copy-Item -Force VERSION dist/SubTranscribeStudio/VERSION

      - name: Zip portable build
        shell: pwsh
        run: Compress-Archive -Path dist/SubTranscribeStudio/* -DestinationPath SubTranscribe-Studio-Windows-Portable.zip

      - name: Install Inno Setup
        run: choco install innosetup -y --no-progress

      - name: Compile installer
        shell: pwsh
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss
          # Copy the compiled installer up to the workspace root so both
          # upload paths below share the same base directory — with entries
          # split across two different directories (repo root vs dist/),
          # upload-artifact uses their lowest common ancestor as the
          # artifact root, which nests the .exe under a dist/ subfolder and
          # makes the release job's flat glob fail to find it silently.
          Copy-Item -Force dist\SubTranscribe_Studio_Setup_*.exe .

      - name: Upload Windows Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SubTranscribe-Studio-Windows
          path: |
            SubTranscribe-Studio-Windows-Portable.zip
            SubTranscribe_Studio_Setup_*.exe

  # ---------------------------------------------------------
  # 2. LINUX BUILD
  # ---------------------------------------------------------
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        run: |
          VER="${GITHUB_REF_NAME#v}"
          if [ -z "$VER" ]; then VER="0.0.0-dev"; fi
          printf '%s' "$VER" > VERSION

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y portaudio19-dev libasound2-dev libx11-dev ffmpeg \
            libxcb-cursor0 libxcb-xinerama0 libxcb-icccm4 libxcb-image0 \
            libxcb-keysyms1 libxcb-render-util0 libxcb-shape0 libegl1 libgl1

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r subtranscribe/requirements.txt
          pip install pyinstaller

      - name: Build Linux Binary with PyInstaller
        run: |
          pyinstaller \
            --name "SubTranscribeStudio" \
            --onedir \
            --collect-all PySide6 \
            --collect-all faster_whisper \
            --collect-all ctranslate2 \
            --collect-all deep_translator \
            --collect-all tiktoken \
            --collect-all tokenizers \
            --collect-all sounddevice \
            --clean \
            --noconfirm \
            main.py

      - name: Bundle Linux runtime assets & Compress
        run: |
          cp -r subtranscribe/assets dist/SubTranscribeStudio/assets
          if [ -d "bin" ]; then cp -r bin dist/SubTranscribeStudio/bin; fi
          cp VERSION dist/SubTranscribeStudio/VERSION
          tar -czvf SubTranscribe-Studio-Linux-x86_64.tar.gz -C dist/SubTranscribeStudio .

      - name: Upload Linux Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SubTranscribe-Studio-Linux
          path: SubTranscribe-Studio-Linux-x86_64.tar.gz

  # ---------------------------------------------------------
  # 3. MACOS BUILD (MacBook / Mac OS)
  # ---------------------------------------------------------
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        run: |
          VER="${GITHUB_REF_NAME#v}"
          if [ -z "$VER" ]; then VER="0.0.0-dev"; fi
          printf '%s' "$VER" > VERSION

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r subtranscribe/requirements.txt
          pip install pyinstaller

      - name: Build macOS App Bundle with PyInstaller
        run: |
          pyinstaller \
            --name "SubTranscribeStudio" \
            --windowed \
            --onedir \
            --collect-all PySide6 \
            --collect-all faster_whisper \
            --collect-all ctranslate2 \
            --collect-all deep_translator \
            --collect-all tiktoken \
            --collect-all tokenizers \
            --collect-all sounddevice \
            --clean \
            --noconfirm \
            main.py

      - name: Bundle macOS runtime assets & Zip
        run: |
          cp -r subtranscribe/assets dist/SubTranscribeStudio/assets
          if [ -d "bin" ]; then cp -r bin dist/SubTranscribeStudio/bin; fi
          cp VERSION dist/SubTranscribeStudio/VERSION
          cd dist && zip -r ../SubTranscribe-Studio-macOS-universal.zip SubTranscribeStudio

      - name: Upload macOS Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SubTranscribe-Studio-macOS
          path: SubTranscribe-Studio-macOS-universal.zip

  # ---------------------------------------------------------
  # 4. RASPBERRY PI OS / LINUX ARM64 BUILD
  # ---------------------------------------------------------
  build-raspberry-pi:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        run: |
          VER="${GITHUB_REF_NAME#v}"
          if [ -z "$VER" ]; then VER="0.0.0-dev"; fi
          printf '%s' "$VER" > VERSION

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y portaudio19-dev libasound2-dev libx11-dev ffmpeg \
            libxcb-cursor0 libxcb-xinerama0 libxcb-icccm4 libxcb-image0 \
            libxcb-keysyms1 libxcb-render-util0 libxcb-shape0 libegl1 libgl1

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r subtranscribe/requirements.txt
          pip install pyinstaller

      - name: Build ARM64 Binary with PyInstaller
        run: |
          pyinstaller \
            --name "SubTranscribeStudio" \
            --onedir \
            --collect-all PySide6 \
            --collect-all faster_whisper \
            --collect-all ctranslate2 \
            --collect-all deep_translator \
            --collect-all tiktoken \
            --collect-all tokenizers \
            --collect-all sounddevice \
            --clean \
            --noconfirm \
            main.py

      - name: Bundle Raspberry Pi runtime assets & Compress
        run: |
          cp -r subtranscribe/assets dist/SubTranscribeStudio/assets
          if [ -d "bin" ]; then cp -r bin dist/SubTranscribeStudio/bin; fi
          cp VERSION dist/SubTranscribeStudio/VERSION
          tar -czvf SubTranscribe-Studio-RaspberryPi-arm64.tar.gz -C dist/SubTranscribeStudio .

      - name: Upload Raspberry Pi Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SubTranscribe-Studio-RaspberryPi
          path: SubTranscribe-Studio-RaspberryPi-arm64.tar.gz

  # ---------------------------------------------------------
  # 5. DOCKER CONTAINER BUILD TEST
  # ---------------------------------------------------------
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: subtranscribe-studio:latest

  # ---------------------------------------------------------
  # 6. PUBLISH MULTI-PLATFORM GITHUB RELEASE
  # ---------------------------------------------------------
  release:
    needs: [build-windows, build-linux, build-macos, build-raspberry-pi, build-docker]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Windows Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: SubTranscribe-Studio-Windows
          path: release_assets/windows

      - name: Download Linux Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: SubTranscribe-Studio-Linux
          path: release_assets/linux

      - name: Download macOS Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: SubTranscribe-Studio-macOS
          path: release_assets/macos

      - name: Download Raspberry Pi Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: SubTranscribe-Studio-RaspberryPi
          path: release_assets/raspberrypi

      - name: Create Multi-Platform GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: SubTranscribe Studio ${{ github.ref_name }}
          body: |
            ## 🚀 SubTranscribe Studio ${{ github.ref_name }}

            **Cross-Platform, local, private, AI-powered subtitle generator, translator & audio intelligence studio**

            ---

            ### 📦 Multi-Platform Download & Installation Assets

            #### 🪟 Windows (10/11)
            - **`SubTranscribe_Studio_Setup_${{ github.ref_name }}.exe`** — Windows Installer *(Recommended: Start Menu shortcut & clean uninstaller)*
            - **`SubTranscribe-Studio-Windows-Portable.zip`** — Windows Portable Bundle *(Unzip and run `SubTranscribeStudio.exe`)*

            #### 🐧 Linux (Ubuntu / Debian / Fedora / Arch)
            - **`SubTranscribe-Studio-Linux-x86_64.tar.gz`** — Linux Binary Bundle *(Extract and run `./SubTranscribeStudio`)*

            #### 🍓 Raspberry Pi OS & ARM64 Linux (Raspberry Pi 4 / 5)
            - **`SubTranscribe-Studio-RaspberryPi-arm64.tar.gz`** — ARM64 Linux Bundle *(Extract and run `./SubTranscribeStudio`)*

            #### 🍎 macOS (MacBook / Mac Intel & Apple Silicon M1/M2/M3/M4)
            - **`SubTranscribe-Studio-macOS-universal.zip`** — macOS Desktop Bundle *(Unzip and open `SubTranscribeStudio`)*

            #### 🐳 Docker Container (NAS / Unraid / Synology / Linux Server)
            - Run with Docker: `docker compose up -d` or `docker build -t subtranscribe-studio .`

            ---

            ### ✨ Key Features across all Platforms

            - 🔒 **100% Private & Offline**: Speech recognition and translation run completely locally on your machine.
            - ⚡ **Cross-Platform Hardware Acceleration**: NVIDIA CUDA, AMD/Intel Vulkan, or CPU quantized inference.
            - 🎙️ **Multi-Format Subtitle Export**: Save as **SRT**, **WebVTT**, **ASS (SubStation Alpha)**, or plain **TXT**.
            - 🌐 **100+ Languages**: Automatic language detection and translation support.
            - 📼 **Batch Processing**: Queue up and process multiple audio/video files automatically.
            - 🎛️ **Live Waveform & Subtitle Studio**: Synchronized waveform audio player with real-time subtitle editing.

            ---

            > **💡 First Run Notes**: 
            > - **Windows**: If SmartScreen flags the unsigned executable, click **More info → Run anyway**.
            > - **macOS**: On first launch, if macOS Gatekeeper flags unverified developer, right-click `SubTranscribeStudio` and click **Open**.
          files: |
            release_assets/windows/SubTranscribe-Studio-Windows-Portable.zip
            release_assets/windows/SubTranscribe_Studio_Setup_*.exe
            release_assets/linux/SubTranscribe-Studio-Linux-x86_64.tar.gz
            release_assets/raspberrypi/SubTranscribe-Studio-RaspberryPi-arm64.tar.gz
            release_assets/macos/SubTranscribe-Studio-macOS-universal.zip
          generate_release_notes: true
